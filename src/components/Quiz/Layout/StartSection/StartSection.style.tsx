import styled from 'styled-components';

// eslint-disable-next-line import/prefer-default-export
export const QuizRules = styled.p`
  text-align: center;
  font-size: ${({ theme }) => theme.fontSizes.md};
  padding: 25px 10px;
`;
